import { IModels } from '~/connectionResolvers';

export const generateConditionStageIds = async (
  models: IModels,
  {
    boardId,
    pipelineId,
    options,
  }: {
    boardId?: string;
    pipelineId?: string;
    options?: any;
  },
) => {};
