export const types = ``;

export const queries = ``;

export const mutations = `
  reCalcRemainders(
    departmentId: String,
    branchId: String,
    productCategoryId: String,
    productIds: [String],
  ): String
`;
