# Node modules — installed fresh in Docker (biggest win: ~2GB excluded from context)
node_modules
**/node_modules

# Git history — not needed at runtime
.git/

# Local env and OS artifacts
.env*
.DS_Store
*.log
