mixin monpay(invoice)
    .isMobile#isMobile
        h3 QR code
            .border.flex-center
                img(src=`${invoice.apiResponse.qrData}`)

    .desktop#desktop
        h3 QR code
            .border.flex-center
                img(src=`${invoice.apiResponse.qrData}`)