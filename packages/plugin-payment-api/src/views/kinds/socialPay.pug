mixin socialpay(invoice)
    
    if invoice.phone
        .label #{invoice.apiResponse.text}
    else
        h3 QR code
            .border.flex-center
                img(src=`${invoice.apiResponse.qrData}`)