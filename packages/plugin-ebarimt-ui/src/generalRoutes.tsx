import React from 'react';
import { BrowserRouter as Router } from 'react-router-dom';
import EbarimtRoutes from './routes';

const Routes = () => (
  <Router>
    <EbarimtRoutes />
  </Router>
);

export default Routes;
