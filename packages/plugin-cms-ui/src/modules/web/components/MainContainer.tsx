import React from 'react'

type Props = {
    currentConfig: any;
}

const MainContainer = (props: Props) => {
  return (
    <div style={{width: '100%', height: '60px', backgroundColor: 'red'}}>MainContainer</div>
  )
}

export default MainContainer