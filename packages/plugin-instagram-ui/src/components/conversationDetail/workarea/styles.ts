import styled from 'styled-components';

const InstagramTaggedMessage = styled.div`
  margin-top: 15px;
`;

const InstagramTagText = styled.p`
  margin-top: 5px;
  a {
    margin: 5px;
  }
`;

export { InstagramTaggedMessage, InstagramTagText };
