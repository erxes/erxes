import * as animations from 'erxes-ui/lib/utils/animations';

const rotate = animations.rotate;
const fadeIn = animations.fadeIn;
const slideDown = animations.slideDown;
const slideLeft = animations.slideLeft;
const slideRight = animations.slideRight;
const shake = animations.shake;
const twinkling = animations.twinkling;
const stripe = animations.stripe;
const wave = animations.wave;
const robotAnimation = animations.robotAnimation;
const highlight = animations.highlight;

export {
  rotate,
  fadeIn,
  slideDown,
  slideLeft,
  slideRight,
  shake,
  twinkling,
  stripe,
  wave,
  robotAnimation,
  highlight
};
