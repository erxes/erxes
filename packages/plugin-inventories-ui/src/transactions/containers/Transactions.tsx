import React from 'react';
import TransactionsComponent from '../components/Transactions';

const Transactions = () => {
  return <TransactionsComponent data={[]} />;
};

export default Transactions;
