import { IModels } from '../../connectionResolver';
import { IContract } from '../definitions/contracts';

function changeClassificationContract(
  contract: IContract,
  payDate: Date,
  models: IModels
) {}

function massChangeClassification(payDate: Date, models: IModels) {}
