import { IModels } from '../../connectionResolver';
import { IContract } from '../definitions/contracts';

function storeInterestContract(
  contract: IContract,
  payDate: Date,
  models: IModels
) {}

function massStoreInterest(payDate: Date, models: IModels) {}
