import { IModels } from '../../connectionResolver';
import { IContract } from '../definitions/contracts';

function checkScheduleContract(
  contract: IContract,
  payDate: Date,
  models: IModels
) {}

function massCheckSchedule(payDate: Date, models: IModels) {}
