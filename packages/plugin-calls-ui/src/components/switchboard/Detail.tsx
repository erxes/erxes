import React, { useEffect, useState } from 'react';
import { __, router } from '@erxes/ui/src/utils';

import Button from '@erxes/ui/src/components/Button';

function Detail() {
  useEffect(() => {}, []);

  return <h1>switchboard detail</h1>;
}

export default Detail;
