import {
  redis,
  join,
  leave,
  getServices,
  getService,
  isEnabled,
  isAvailable
} from '@erxes/api-utils/src/serviceDiscovery';

export { redis, getServices, getService, join, isEnabled, leave, isAvailable };
