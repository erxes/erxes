import { IContext } from '../../connectionResolver';
import { ITumentechDeal } from '../../models/definitions/tumentechDeal';

const TumentechDeal = {
  // async deal(dealId: ITumentechDeal, params, { models, subdomain }: IContext) {
  //   return (
  //     dealId && {
  //       __typename: "Deal",
  //       _id: dealId,
  //     }
  //   );
  // },
};

export { TumentechDeal };
