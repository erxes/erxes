{
  "name": "plugin-mobinet-ui",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "cd .erxes && yarn install",
    "start": "cd .erxes && yarn start",
    "build": "cd .erxes && yarn build"
  },
  "dependencies": {
    "leaflet": "^1.9.3",
    "react-leaflet": "^4.2.0"
  },
  "devDependencies": {
    "@types/react-leaflet": "^2.8.2"
  }
}
