// test failed info: IDeal
// import { mount, shallow } from 'enzyme';
// import * as React from 'react';
// import DealEditForm from '../../../../modules/deals/components/editForm/EditForm';
// import { IDealParams } from 'modules/deals/types';

// describe('DealEditForm component', () => {
//   const defaultProps = {
//     deal: IDeal,
//     users: IUser[],
//     addDeal: (doc: IDealParams, callback: () => void, msg?: string) => null,
//     saveDeal: (doc: IDealParams, callback: () => void) => null,
//     removeDeal: (dealId: string, callback: () => void) => null,
//     closeModal: () => null,
//   };

//   test('renders successfully', () => {
//     shallow(<DealEditForm {...defaultProps} />);
//   });

//   test('renders with default props', () => {
//     const control = mount(<DealEditForm {...defaultProps} />);
//     const props = control.props();

//     expect(props).toMatchObject(defaultProps);
//   });
// });
