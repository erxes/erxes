import styled from 'styled-components';
import { colors } from 'modules/common/styles';

const EngageTitle = styled.div`
  font-weight: 400;
`;

const HelperText = styled.div`
  color: ${colors.colorCoreGray};
  font-size: 12px;
`;

export { EngageTitle, HelperText };
