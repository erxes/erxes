import React, { Component } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

class IntroPreview extends Component {
  render() {
    return <div>hi</div>;
  }
}

IntroPreview.propTypes = propTypes;

export default IntroPreview;
