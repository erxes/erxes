// import * as React from 'react';

type Props = {
  queryParams: any;
};

const WithPipelinesQuery = (props: Props) => {
  // const { queryParams } = props;

  // const pipeline = { _id: 'pipeline1', name: 'Pipeline 1' };

  return null;
};

export default WithPipelinesQuery;
