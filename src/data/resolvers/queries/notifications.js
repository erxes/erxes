import { MODULE_LIST } from '../../constants';

export default {
  /**
   * Module list used in notifications
   * @param {Object} args
   * @return {Promise} module list
   */
  notifiationsModules() {
    return MODULE_LIST;
  },
};
