import { Badge } from 'erxes-ui';
import { SpecificFieldProps } from './Field';

export const FieldFile = (props: SpecificFieldProps) => {
  return (
    <div className="flex items-center gap-2 px-2">
      File <Badge variant="secondary">coming soon</Badge>
    </div>
  );
};
