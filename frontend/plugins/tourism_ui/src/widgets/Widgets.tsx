const Widgets = ({
  contentType,
  contentId,
  message,
}: {
  contentType: string;
  contentId: string;
  message: string;
}) => {
  return <div>Widgets tourism {message}</div>;
};

export default Widgets;
