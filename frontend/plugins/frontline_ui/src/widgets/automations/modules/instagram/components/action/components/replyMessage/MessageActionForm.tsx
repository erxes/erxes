// Instagram message action form reuses the Facebook message action form
// as the message format (text, cards, quick replies, etc.) is identical
export {
  MessageActionForm,
} from '~/widgets/automations/modules/facebook/components/action/components/replyMessage/MessageActionForm';
