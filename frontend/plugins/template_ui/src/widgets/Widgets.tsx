export const Widgets = ({
  module,
  contentId,
  contentType,
}: {
  module: any;
  contentId: string;
  contentType: string;
}) => {
  return <div>template Widget</div>;
};

export default Widgets;
