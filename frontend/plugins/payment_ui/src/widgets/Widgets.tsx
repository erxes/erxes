export const Widgets = ({
  module,
  contentId,
  contentType,
}: {
  module: any;
  contentId: string;
  contentType: string;
}) => {
  return <div>payment Widget</div>;
};

export default Widgets;
