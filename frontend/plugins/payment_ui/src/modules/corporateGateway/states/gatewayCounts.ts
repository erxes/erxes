import { atom } from 'jotai';

export const golomtConfigsCountAtom = atom<number | null>(null);
export const khanbankConfigsCountAtom = atom<number | null>(null);
export const tdbConfigsCountAtom = atom<number | null>(null);
