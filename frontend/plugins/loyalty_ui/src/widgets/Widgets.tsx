export const Widgets = ({
  module,
  contentId,
  contentType,
}: {
  module: any;
  contentId: string;
  contentType: string;
}) => {
  return <div>pricing Widget</div>;
};

export default Widgets;
