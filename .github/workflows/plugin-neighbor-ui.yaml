name: Plugin neighbor UI CI

on:
  push:
    branches:
      - dev
      - staging
      - build-test
    paths:
      - 'packages/erxes-ui/**'
      - 'packages/ui-plugin-template/**'
      - 'packages/ui-cards/**'
      - 'packages/ui-forms/**'
      - 'packages/ui-inbox/**'
      - 'packages/ui-knowledgebase/**'
      - 'packages/ui-leads/**'
      - 'packages/ui-log/**'
      - 'packages/ui-notifications/**'
      - 'packages/ui-products/**'
      - 'packages/ui-segments/**'
      - 'packages/ui-settings/**'
      - 'packages/ui-team/**'
      - 'packages/ui-neighbor/**'
      - 'packages/plugin-neighbor-ui/**'
      - '.github/workflows/plugin-neighbor-ui.yaml'
  pull_request:
    branches:
      - dev
      - staging
      - build-test
    paths:
      - 'packages/erxes-ui/**'
      - 'packages/ui-plugin-template/**'
      - 'packages/ui-cards/**'
      - 'packages/ui-forms/**'
      - 'packages/ui-inbox/**'
      - 'packages/ui-knowledgebase/**'
      - 'packages/ui-leads/**'
      - 'packages/ui-log/**'
      - 'packages/ui-notifications/**'
      - 'packages/ui-products/**'
      - 'packages/ui-segments/**'
      - 'packages/ui-settings/**'
      - 'packages/ui-team/**'
      - 'packages/ui-neighbor/**'
      - 'packages/plugin-neighbor-ui/**'
      - '.github/workflows/plugin-neighbor-ui.yaml'

jobs:
  ui:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js 18.17.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.17.x

      - name: Build
        run: |
          yarn install --frozen-lockfile
          cd packages/plugin-neighbor-ui
          yarn build
          cp -r ./src/locales/ ./dist/locales 2>/dev/null || true

      - name: Configure AWS credentials
        if: github.event_name == 'push' && ( github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/staging' || github.ref == 'refs/heads/build-test')
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Deploy
        if: github.event_name == 'push' && ( github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/staging' || github.ref == 'refs/heads/build-test')
        run: |
          tar -cf build.tar --directory=packages/plugin-neighbor-ui/dist .
          cp build.tar packages/plugin-neighbor-ui/dist
          rm -rf packages/plugin-neighbor-ui/dist/*.js
          aws s3 sync packages/plugin-neighbor-ui/dist s3://erxes-enterprise-plugins/uis/plugin-neighbor-ui --delete
