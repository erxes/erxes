name: Plugin mobinet Api CI

on:
  push:
    branches:
      - dev
      - staging
    paths:
      - 'packages/api-utils/**'
      - 'packages/api-plugin-template.erxes/**'
      - 'packages/plugin-mobinet-api/**'
      - '.github/workflows/plugin-mobinet-api.yaml'
  pull_request:
    branches:
      - dev
      - staging
    paths:
      - 'packages/api-utils/**'
      - 'packages/api-plugin-template.erxes/**'
      - 'packages/plugin-mobinet-api/**'
      - '.github/workflows/plugin-mobinet-api.yaml'

jobs:
  api:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js 14.20.x
        uses: actions/setup-node@v1
        with:
          node-version: 14.20.x

      # https://github.com/actions/cache/blob/main/examples.md#node---yarn
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v2
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-plugin-mobinet-api-${{ hashFiles('yarn.lock', 'plugin-mobinet-api/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-plugin-mobinet-api-
            ${{ runner.os }}-yarn-

      - name: Build
        run: |
          cp -r builder dist
          cd dist
          yarn install
          yarn build plugin mobinet
          cd plugin-mobinet-api/.erxes
          cd dist/main/.erxes/src

      - name: Build docker image
        if: github.event_name == 'push' && ( github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/staging')
        run: |
          cd dist/plugin-mobinet-api/.erxes
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login https://registry.erxes.io -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker build -t registry.erxes.io/erxes/plugin-mobinet-api:enterprise -f Dockerfile .
          docker push registry.erxes.io/erxes/plugin-mobinet-api:enterprise
