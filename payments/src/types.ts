export interface IRouterProps {
  history: any;
  location: any;
  match: any;
}

export interface IQueryParams {
  amount: number;
  customerId: string;
  companyId: string;
  contentType: string;
  contentTypeId: string;
}
